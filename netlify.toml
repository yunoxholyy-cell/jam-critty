[build]
  command = "pnpm --filter @workspace/jam-critty run build"
  publish = "artifacts/jam-critty/dist/public"

[build.environment]
  NODE_VERSION = "24"
  PNPM_VERSION = "10"

# SPA fallback — all routes serve index.html
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
